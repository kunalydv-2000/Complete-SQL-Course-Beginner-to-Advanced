# Lesson 08: Building a Sample Database

> Apply everything you've learned in Module 02 by building a complete Library Management System database from scratch using SQL Server.

---

# Learning Objectives

After completing this lesson, you will be able to:

* Create a complete database
* Design multiple related tables
* Apply data types correctly
* Implement constraints
* Create primary and foreign keys
* Insert sample data
* Verify relationships
* Build a real-world database solution

---

# Introduction

Throughout this module, you learned:

✅ Creating databases

✅ Working with data types

✅ Creating tables

✅ Applying constraints

✅ Inserting data

✅ Updating data

✅ Deleting data

Now it's time to combine everything into a complete project.

In this lesson, you'll build a **Library Management System** database.

---

# Business Scenario

A local library currently manages information manually.

The library wants to track:

* Books
* Authors
* Members
* Borrowing Records
* Reservations
* Publishers
* Categories

The goal is to create a relational database that supports daily operations.

---

# Database Design

The database will contain:

```text id="db001"
LibraryManagementSystem
│
├── Categories
├── Publishers
├── Authors
├── Books
├── BookAuthors
├── Members
├── BorrowRecords
└── Reservations
```

---

# Step 1: Create the Database

```sql id="db002"
CREATE DATABASE LibraryManagementSystem;
GO

USE LibraryManagementSystem;
GO
```

---

# Why Create a Database First?

A database acts as a container for:

* Tables
* Views
* Stored Procedures
* Functions
* Security Objects

---

# Step 2: Create Categories Table

Purpose:

Store book categories.

Examples:

```text id="db003"
Technology
Science
Business
History
Fiction
```

---

SQL:

```sql id="db004"
CREATE TABLE Categories (
    CategoryID INT IDENTITY(1,1) PRIMARY KEY,
    CategoryName VARCHAR(100) NOT NULL UNIQUE
);
```

---

# Table Structure

| Column       | Data Type    |
| ------------ | ------------ |
| CategoryID   | INT          |
| CategoryName | VARCHAR(100) |

---

# Step 3: Create Publishers Table

Purpose:

Store publisher information.

---

SQL:

```sql id="db005"
CREATE TABLE Publishers (
    PublisherID INT IDENTITY(1,1) PRIMARY KEY,
    PublisherName VARCHAR(150) NOT NULL,
    Country VARCHAR(100)
);
```

---

# Example Data

| PublisherName  | Country |
| -------------- | ------- |
| O'Reilly Media | USA     |
| Pearson        | USA     |
| Packt          | UK      |

---

# Step 4: Create Authors Table

Purpose:

Store author information.

---

SQL:

```sql id="db006"
CREATE TABLE Authors (
    AuthorID INT IDENTITY(1,1) PRIMARY KEY,
    FirstName VARCHAR(100) NOT NULL,
    LastName VARCHAR(100) NOT NULL,
    BirthDate DATE
);
```

---

# Step 5: Create Books Table

Purpose:

Store book details.

---

SQL:

```sql id="db007"
CREATE TABLE Books (
    BookID INT IDENTITY(1,1) PRIMARY KEY,

    Title VARCHAR(255) NOT NULL,

    ISBN VARCHAR(20) UNIQUE NOT NULL,

    PublicationYear INT,

    CategoryID INT,

    PublisherID INT,

    CONSTRAINT FK_Books_Category
    FOREIGN KEY (CategoryID)
    REFERENCES Categories(CategoryID),

    CONSTRAINT FK_Books_Publisher
    FOREIGN KEY (PublisherID)
    REFERENCES Publishers(PublisherID)
);
```

---

# Understanding Relationships

```text id="db008"
Categories
     │
     ▼
Books
     ▲
     │
Publishers
```

One category can contain many books.

One publisher can publish many books.

---

# Step 6: Create BookAuthors Table

A book can have multiple authors.

An author can write multiple books.

This is a Many-to-Many relationship.

---

Relationship:

```text id="db009"
Books
   ↔
BookAuthors
   ↔
Authors
```

---

SQL:

```sql id="db010"
CREATE TABLE BookAuthors (
    BookID INT NOT NULL,

    AuthorID INT NOT NULL,

    PRIMARY KEY (BookID, AuthorID),

    CONSTRAINT FK_BookAuthors_Book
        FOREIGN KEY (BookID)
        REFERENCES Books(BookID),

    CONSTRAINT FK_BookAuthors_Author
        FOREIGN KEY (AuthorID)
        REFERENCES Authors(AuthorID)
);
```

---

# Step 7: Create Members Table

Purpose:

Store member information.

---

SQL:

```sql id="db011"
CREATE TABLE Members (
    MemberID INT IDENTITY(1,1) PRIMARY KEY,

    FirstName VARCHAR(100) NOT NULL,

    LastName VARCHAR(100) NOT NULL,

    Email VARCHAR(255) UNIQUE,

    Phone VARCHAR(20),

    MembershipDate DATE
        DEFAULT GETDATE()
);
```

---

# Step 8: Create BorrowRecords Table

Purpose:

Track borrowed books.

---

SQL:

```sql id="db012"
CREATE TABLE BorrowRecords (
    BorrowID INT IDENTITY(1,1) PRIMARY KEY,

    MemberID INT NOT NULL,

    BookID INT NOT NULL,

    BorrowDate DATE NOT NULL,

    DueDate DATE NOT NULL,

    ReturnDate DATE,

    CONSTRAINT FK_Borrow_Member
        FOREIGN KEY (MemberID)
        REFERENCES Members(MemberID),

    CONSTRAINT FK_Borrow_Book
        FOREIGN KEY (BookID)
        REFERENCES Books(BookID)
);
```

---

# Relationship Diagram

```text id="db013"
Members
    │
    ▼
BorrowRecords
    ▲
    │
Books
```

---

# Step 9: Create Reservations Table

Purpose:

Track reserved books.

---

SQL:

```sql id="db014"
CREATE TABLE Reservations (
    ReservationID INT IDENTITY(1,1) PRIMARY KEY,

    MemberID INT NOT NULL,

    BookID INT NOT NULL,

    ReservationDate DATE NOT NULL,

    Status VARCHAR(20)
        DEFAULT 'Pending',

    CONSTRAINT FK_Reservation_Member
        FOREIGN KEY (MemberID)
        REFERENCES Members(MemberID),

    CONSTRAINT FK_Reservation_Book
        FOREIGN KEY (BookID)
        REFERENCES Books(BookID)
);
```

---

# Database Schema Overview

```text id="db015"
Categories
      │
      ▼
Books
      ▲
      │
Publishers

Books
  ↔
BookAuthors
  ↔
Authors

Books
  ↔
BorrowRecords
  ↔
Members

Books
  ↔
Reservations
  ↔
Members
```

---

# Step 10: Insert Categories

```sql id="db016"
INSERT INTO Categories
(CategoryName)
VALUES
('Technology'),
('Science'),
('Business'),
('History'),
('Fiction');
```

---

# Step 11: Insert Publishers

```sql id="db017"
INSERT INTO Publishers
(PublisherName, Country)
VALUES
('O''Reilly Media', 'USA'),
('Packt Publishing', 'UK'),
('Pearson', 'USA');
```

---

# Step 12: Insert Authors

```sql id="db018"
INSERT INTO Authors
(
    FirstName,
    LastName,
    BirthDate
)
VALUES
(
    'Alan',
    'Beaulieu',
    '1965-05-10'
),
(
    'Robert',
    'Martin',
    '1952-12-05'
);
```

---

# Step 13: Insert Books

```sql id="db019"
INSERT INTO Books
(
    Title,
    ISBN,
    PublicationYear,
    CategoryID,
    PublisherID
)
VALUES
(
    'Learning SQL',
    '9780596520830',
    2020,
    1,
    1
),
(
    'Clean Code',
    '9780132350884',
    2008,
    3,
    3
);
```

---

# Step 14: Insert Members

```sql id="db020"
INSERT INTO Members
(
    FirstName,
    LastName,
    Email,
    Phone
)
VALUES
(
    'John',
    'Smith',
    'john@email.com',
    '9876543210'
),
(
    'Sarah',
    'Wilson',
    'sarah@email.com',
    '9876543211'
);
```

---

# Step 15: Insert Borrow Records

```sql id="db021"
INSERT INTO BorrowRecords
(
    MemberID,
    BookID,
    BorrowDate,
    DueDate
)
VALUES
(
    1,
    1,
    GETDATE(),
    DATEADD(DAY, 14, GETDATE())
);
```

---

# Step 16: Verify Data

Run:

```sql id="db022"
SELECT * FROM Categories;
SELECT * FROM Publishers;
SELECT * FROM Authors;
SELECT * FROM Books;
SELECT * FROM Members;
SELECT * FROM BorrowRecords;
```

---

# Testing Relationships

Attempt:

```sql id="db023"
INSERT INTO BorrowRecords
(
    MemberID,
    BookID,
    BorrowDate,
    DueDate
)
VALUES
(
    999,
    1,
    GETDATE(),
    DATEADD(DAY, 14, GETDATE())
);
```

---

Result:

```text id="db024"
Foreign Key Constraint Error
```

This proves the relationship is working.

---

# Updating Data

Example:

```sql id="db025"
UPDATE Members
SET Email = 'johnsmith@email.com'
WHERE MemberID = 1;
```

---

# Deleting Data

Example:

```sql id="db026"
DELETE FROM Reservations
WHERE ReservationID = 1;
```

---

# Final Database Verification

Check all tables:

```sql id="db027"
SELECT *
FROM INFORMATION_SCHEMA.TABLES;
```

Expected:

```text id="db028"
Categories
Publishers
Authors
Books
BookAuthors
Members
BorrowRecords
Reservations
```

---

# Project Challenge

Extend the database by adding:

### Reviews

```text id="db029"
ReviewID
MemberID
BookID
Rating
Comments
```

---

### Library Staff

```text id="db030"
StaffID
FirstName
LastName
Position
```

---

### Branches

```text id="db031"
BranchID
BranchName
Location
```

Create relationships where appropriate.

---

# Best Practices Applied

Throughout this project you used:

✅ Meaningful table names

✅ Appropriate data types

✅ Primary keys

✅ Foreign keys

✅ Unique constraints

✅ Default values

✅ Normalized structure

---

# Summary

In this lesson, you built a complete Library Management System database and applied everything learned in Module 02:

* Database creation
* Table creation
* Data types
* Constraints
* Relationships
* Data insertion
* Data updates
* Data deletion

This is your first fully functional relational database project.

---

# Knowledge Check

### Question 1

Which table resolves the many-to-many relationship between Books and Authors?

A. Members

B. BorrowRecords

C. BookAuthors

D. Categories

---

### Question 2

Which constraint enforces relationships?

A. UNIQUE

B. CHECK

C. FOREIGN KEY

D. DEFAULT

---

### Question 3

What does IDENTITY(1,1) do?

A. Creates a relationship

B. Generates automatic values

C. Creates a constraint

D. Creates an index

---

### Question 4

Which statement verifies created tables?

```sql
SELECT *
FROM INFORMATION_SCHEMA.TABLES;
```

A. True

B. False

---

# Module 02 Completion

🎉 Congratulations!

You have completed **Module 02: Creating and Populating Databases**.

You can now:

✅ Create databases

✅ Create tables

✅ Choose data types

✅ Apply constraints

✅ Insert data

✅ Update data

✅ Delete data

✅ Build complete relational database solutions

---

# Next Module

➡️ **Module 03: Query Fundamentals**

In the next module, you'll learn how to retrieve data using:

* SELECT
* FROM
* WHERE
* ORDER BY
* DISTINCT
* TOP
* Aliases

and begin answering business questions using SQL queries.
