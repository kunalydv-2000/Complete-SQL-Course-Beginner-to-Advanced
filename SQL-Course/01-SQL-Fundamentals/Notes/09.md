# SQL Query Processing Order

Consider the query:

```sql
SELECT Name
FROM Employees
WHERE Salary > 50000;
```

## Logical Execution Order

1. FROM
2. WHERE
3. SELECT

Although SELECT appears first, the database processes FROM and WHERE before SELECT.

Understanding execution order is important for advanced SQL development.